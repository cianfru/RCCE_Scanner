import React from "react";
import ReactDOM from "react-dom/client";
import { RefinedLanding } from "./RefinedLanding.tsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RefinedLanding />
  </React.StrictMode>
);
