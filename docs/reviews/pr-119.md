# PR #119 review

Reviewed branch `claude/project-review-costs-kweeki`, head
`2f4781c013df4da0460987f6bb1c11b1b860172e`, against main.
Leave unmerged until these findings are addressed.

1. **P1 — rate-limiter key cap is not enforced.** `backend/ratelimit.py:40-47`
   inserts the new key then only prunes expired keys. A stream of fresh identities
   retains every active key beyond `_MAX_KEYS`; each subsequent request also scans
   that growing dictionary while holding a synchronous lock. Reproduced with a
   cap of 2: three fresh keys remain. Reject new identities at capacity (without
   evicting their rate-limit history) or use a bounded fallback/global budget;
   add an active-key saturation test. The comment currently promises a bound the
   implementation does not provide.
2. **P2 — existing scheduler tests all fail.** `backend/scan_schedule.py:4,28`
   removes `kind`/`kind_for`, but `backend/tests/test_scan_schedule.py` was not
   updated. Ran the branch's scheduler against its unchanged five tests: five
   TypeErrors. Update calls and retain coverage for timing, idle transitions,
   unavailable-history backoff, initial coverage and fairness.

The cached coin map, bounded assistant session retention and dead-code/landing
cleanup are reasonable changes. The idle HyperLens change intentionally stops
position observations while idle; document the resulting gaps in wallet history
rather than describing it solely as a UI optimization. Validate proxy-header
trust before relying on the leftmost X-Forwarded-For value for abuse protection;
this review did not establish Railway's overwrite/append behavior, so this is a
verification item rather than a confirmed bypass finding.

The separate spot-quality change is based on main and does not merge PR #119.
After either branch lands, reconcile the overlapping universe/scheduler code and
run the full tests again.
