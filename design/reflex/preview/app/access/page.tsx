'use client';
import {useState} from 'react';
import Link from 'next/link';
import {ArrowLeft,ArrowRight,Wallet,ShieldCheck,Check,LockKeyhole,Info,RefreshCw} from 'lucide-react';
import {Brand} from '@/components/reflex-brand';
import {Tabs,TabsList,TabsTrigger,TabsContent} from '@/components/ui/tabs';
export default function Access(){const[state,setState]=useState('eligible');return <div className="container"><header className="site-nav"><Brand/><Link href="/scanner" className="btn small secondary">Explore demo <ArrowRight size={15}/></Link></header><main className="access-page"><Link href="/#access" className="text-link"><ArrowLeft size={14}/> Back to the platform</Link><div className="access-page-heading"><div className="eyebrow">Reflex / Proposed token utility</div><h1>Hold the token.<br/><span>Unlock the platform.</span></h1><p>At least X Reflex tokens, held in your own wallet.<br/>One threshold for access to the complete workspace.</p></div><div className="access-notice"><Info size={18}/><p>This is an access-flow preview. The token, network, and holding threshold are not configured. No wallet connection, signature, or transaction is requested.</p></div><div className="access-layout"><section className="access-model"><div className="eyebrow">How it works</div>{[[Wallet,'01','Hold at least X','Keep the required token balance in a supported wallet. Tokens stay in your custody.'],[ShieldCheck,'02','Verify ownership','Connect your wallet and sign an authentication message to prove ownership. No token approval is required.'],[LockKeyhole,'03','Access the platform','The platform checks your eligible balance. Access continues while you meet the holding requirement.']].map(([Icon,n,t,d])=>{const I=Icon as typeof Wallet;return <article key={String(n)}><div className="model-icon"><I size={20}/></div><div><span className="mono muted">{String(n)}</span><h2>{String(t)}</h2><p>{String(d)}</p></div></article>})}<div className="holding-rule"><span className="mono">THE ACCESS RULE</span><strong>Eligible balance ≥ X</strong><p>No staking. No token spending. No recurring token payment.</p></div></section><section className="access-flow"><div className="flow-heading"><span className="eyebrow">Interactive preview</span><span className="pill amber">Simulation</span></div><h2>What access looks like.</h2><p>Choose a scenario to preview the experience.</p><Tabs value={state} onValueChange={v=>setState(String(v))}><TabsList className="access-scenarios" aria-label="Access preview scenario"><TabsTrigger value="eligible">Eligible</TabsTrigger><TabsTrigger value="below">Below X</TabsTrigger><TabsTrigger value="unavailable">Check unavailable</TabsTrigger></TabsList><TabsContent value="eligible"><div className="access-state"><span className="state-icon"><ShieldCheck size={30}/></span><span className="pill green">Access granted · Example</span><h3>You’re in.</h3><p>Your example wallet meets the holding requirement. The full workspace is unlocked.</p><div className="balance-line"><span>Illustrative balance</span><strong className="mono">1.2 × X</strong></div><div className="balance-line"><span>Required balance</span><strong className="mono">X tokens</strong></div><Link className="btn" href="/scanner">Open the scanner demo <ArrowRight size={17}/></Link></div></TabsContent><TabsContent value="below"><div className="access-state below"><span className="state-icon"><LockKeyhole size={30}/></span><span className="pill amber">More tokens needed · Example</span><h3>A little short of access.</h3><p>Your example balance is below X. Reach the holding requirement, then verify again.</p><div className="balance-line"><span>Illustrative balance</span><strong className="mono">0.6 × X</strong></div><div className="balance-line"><span>Additional balance needed</span><strong className="mono">0.4 × X</strong></div><button className="btn secondary" onClick={()=>setState('eligible')}><RefreshCw size={16}/> Preview a qualifying balance</button><small>Scenario switch only. No token purchase or balance check occurs.</small></div></TabsContent><TabsContent value="unavailable"><div className="access-state unavailable"><span className="state-icon"><RefreshCw size={30}/></span><span className="pill amber">Verification unavailable · Example</span><h3>Let’s try that again.</h3><p>The balance service couldn’t be reached. This does not mean your wallet is below the threshold.</p><div className="balance-line"><span>Balance status</span><strong>Unknown</strong></div><div className="balance-line"><span>Tokens in your wallet</span><strong>Unaffected</strong></div><button className="btn secondary" onClick={()=>setState('eligible')}><RefreshCw size={16}/> Preview a successful retry</button><small>Scenario switch only. No network request occurs.</small></div></TabsContent></Tabs><div className="flow-footnote"><Check size={14}/> Your tokens never leave your wallet.</div></section></div><section className="access-terms"><div className="eyebrow">Before launch</div><h2>The details we will publish.</h2><div className="terms-grid"><article><span className="mono">01</span><h3>The threshold</h3><p>The exact value of X, the supply context, and how any future threshold changes will be announced.</p></article><article><span className="mono">02</span><h3>Eligible balances</h3><p>The canonical token identifier, supported network and wallet balances, plus whether linked HyperCore balances count.</p></article><article><span className="mono">03</span><h3>Verification rules</h3><p>How often balances are checked, how transfers affect access, and what happens during service outages.</p></article></div></section></main><footer className="site-footer"><div className="footer-bottom" style={{marginTop:0}}><span>© 2026 Reflex</span><span>Proposed utility · X is not yet set · No token sale is live here.</span></div></footer></div>}
